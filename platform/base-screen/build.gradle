plugins {
	id "com.android.library"
	id "kotlin-android"
}

dependencies {
	implementation project(":platform:stdlib-android:lifecycle-aware-lazy")

	api dep.androidx.appCompat

	compileOnly dep.androidx.annotation
}